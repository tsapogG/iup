<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/weight_silhouette_for_medical_sport_practice_svgrepo_com"
        android:label="@string/app_name"
        android:roundIcon="@drawable/weight_silhouette_for_medical_sport_practice_svgrepo_com"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.IUP"
        tools:targetApi="31">
        <activity android:name=".WorkoutDetailActivity" />
        <activity android:name=".RestDayActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustNothing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>